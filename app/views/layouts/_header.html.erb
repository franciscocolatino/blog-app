<nav class="navbar navbar-expand-lg navbar-light bg-light px-4">
  <div class="container-fluid">
    <% if @current_user %>
      <a class="navbar-brand" href="/">Olá, <strong><%= @current_user.name %></strong></a>
    <% end %>

    <div class="collapse navbar-collapse justify-content-end">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/posts">Posts</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/users">Usuários</a>
        </li>
        <% if @current_user %>
          <li class="nav-item">
            <%= link_to "Logout", logout_path, method: :get, class: "btn btn-danger ms-2" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", login_path, method: :get, class: "btn btn-success ms-2" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
